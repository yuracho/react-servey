import React from "react";

const SurveyEnd = () => {
  return (
    <div className="wrap">
      <h2>설문조사에 응해주셔서 감사합니다.</h2>
      <p>소중한 시간내어 설문에 응해주셔서 진심으로 감사드립니다.</p>
    </div>
  );
};

export default SurveyEnd;
